import { LoadingScreen } from '@/components/ui'

export default function ScheduleLoading() {
  return (
    <LoadingScreen
      title="Chalking up the court…"
      detail="Loading the timetable, courts and time slots. 🏸"
    />
  )
}
