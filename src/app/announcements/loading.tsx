import { LoadingScreen } from '@/components/ui'

export default function AnnouncementsLoading() {
  return <LoadingScreen title="Checking the notice board…" detail="Loading the latest word from the committee. 📣" />
}
