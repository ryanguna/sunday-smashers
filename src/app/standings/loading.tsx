import { LoadingScreen } from '@/components/ui'

export default function StandingsLoading() {
  return <LoadingScreen title="Counting up the wins…" detail="Working out who is top of the table. 🏆" />
}
