import { LoadingScreen } from '@/components/ui'

export default function AwardsLoading() {
  return <LoadingScreen title="Polishing the trophies…" detail="Loading champions, runners-up and the MVPs. 🏅" />
}
