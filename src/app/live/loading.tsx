import { LoadingScreen } from '@/components/ui'

export default function LiveLoading() {
  return <LoadingScreen title="Listening for the next rally…" detail="Connecting to the live scores from every court. 🏸" />
}
